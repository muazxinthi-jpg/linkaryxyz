# V3 CI scope

Protected PR CI must pass repository regressions, Worker TypeScript, authenticated app TypeScript, Worker Wrangler dry run and public overlay Wrangler dry run before merge. Production migration 0045 is applied only after the merged production code is healthy.
